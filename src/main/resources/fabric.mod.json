{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "name": "${mod_name}",
  "version": "${mod_version}",
  "description": "${mod_description}",
  "environment": "client",
  "mixins": ["rust.mixins.json"],
  "entrypoints": {
    "client": ["dev.rdh.rust.RUST"]
  }
}